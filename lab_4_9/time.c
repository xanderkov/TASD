#include "defines.h"
#include "array_operations.h"
#include "io.h"
#include "list_operations.h"

void compare_time()
{
    stack arr;
    for (int i = 0; i < 50; i++)
    {
        push(&arr, c);
    }
}